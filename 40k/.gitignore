# Godot 4+ specific ignores
.godot/
/android/

# Local development config (contains local server URL)
server_config.local.json

# Export builds
exports/

# Game data CSVs are generated from the 40kdc 11e dataset (see
# scripts/40kdc/) and ARE committed. Only Godot's import metadata stays local.
data/*.csv.import
