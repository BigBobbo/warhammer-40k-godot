# Day 1 — Finish RNG seed plumbing (#329) + determinism smoke test

Foundation for the scaled feature-validation system. See `.llm/scaled-testing-plan.md` for the multi-day plan.

Audit memory `project_audit_2026_05.md` says #329 has only `MovementPhase.gd:1381` honoring `payload.rng_seed`. That is **stale** — a recent grep across `phases/` shows MovementPhase, ChargePhase, FightPhase all already plumb `payload.rng_seed` through `RNGService` (multiple sites each). The remaining real-gameplay bypasses are smaller than the memory implies; this task closes them and adds the smoke test.

Validator: `bash 40k/tests/run_pretrigger_tests.sh` stays green, plus a new headless determinism smoke test passes with identical dice sequences across 3 runs.

---

- [ ] **Audit `randi()` / `randf()` bypasses across phases + autoloads + scripts**

  Already done — captured in `/tmp/audit_329.txt` summary below. Real remaining bypasses (gameplay-affecting):

  1. `40k/autoloads/TransportManager.gd:352` — `randi_range(1, 6)` for emergency disembark D6
  2. `40k/autoloads/MissionManager.gd:571` — `randi() %` for Supply Drop objective removal (Crucible)
  3. `40k/phases/FightPhase.gd:1887` — `"seed": randi()` for Mathhammer prediction (UI-only display, lower priority)

  Excluded from #329 scope (visual-only, UUIDs, AI tactical noise — none of these affect rules-engine outcomes a scenario would assert on):
  - `DamageFeedbackVisual.gd`, `DiceSoundManager.gd` — visual jitter
  - `GameState.gd:195-199`, `CloudStorage.gd:102-104`, `ReplayManager.gd:502` — UUID generation
  - `AIPlayer.gd`, `AIDecisionMaker.gd` — AI decision noise (separate concern; deterministic AI is a different feature)
  - `NetworkManager.gd:839` — generates the seed for outbound multiplayer actions; this is the seed source itself

- [ ] **Plumb `RNGService` into `TransportManager.disembark`**

  `TransportManager.gd` around line 340-360. The `disembark` function currently does `var roll = randi_range(1, 6)` per model. Pattern to apply (mirroring the phases): accept an optional `rng_seed: int = -1` parameter, instantiate `RulesEngine.RNGService.new(rng_seed)`, replace `randi_range(1, 6)` with `rng.randi_range(1, 6)`. Update callers in `MovementPhase.gd` (search for `disembark` calls there) to forward `action.payload.rng_seed`.

- [ ] **Plumb `RNGService` into `MissionManager` Supply Drop**

  `MissionManager.gd` around line 565-580. The Supply Drop mechanic in Crucible removes random NML objectives at the start of each round. Replace the raw `randi()` with `RNGService` honoring `test_mode_seed`. Since this fires on phase transition (not a player action), the cleanest path is to rely on `RNGService.test_mode_seed` (already static) — `var rng = RulesEngine.RNGService.new()` will pick up the global seed when set.

- [ ] **(Lower priority) Plumb Mathhammer seed**

  `FightPhase.gd:1887` is for a UI-side prediction display. Replace `randi()` with `RulesEngine.RNGService.new().randi()` so it picks up `test_mode_seed`. This means scenarios with `screenshot_match` against Mathhammer UI will be deterministic. Optional for Day 1 if running short.

- [ ] **Add headless determinism smoke test**

  New file: `40k/tests/test_rng_determinism.gd` extending `SceneTree`. Pattern (uses the post-charge fixture which has both pre-positioned units AND a charge sequence to roll):
  1. Load `co_pretrigger.w40ksave`, set `meta.from_save = true`
  2. `RulesEngine.set_test_seed(42)`
  3. Drive a known action sequence that hits 5+ dice rolls (charge roll, hit roll, wound roll, save roll, FNP). Capture each result's `dice` field.
  4. Reset the seed to 42, replay the same actions, capture results again
  5. `_check("Run 1 == Run 2", run1 == run2)` — full deep-equal on the dice arrays
  6. Repeat with seed=42 a third time, also assert equal
  7. Repeat with seed=43 and assert NOT equal (proves the seed is actually being used, not just a constant)

  Wire into `run_pretrigger_tests.sh` TESTS array.

- [ ] **Update audit memory file**

  `~/.claude/projects/-Users-robertocallaghan-Documents-claude-godotv2/memory/project_audit_2026_05.md` line about #329 currently says only MovementPhase honors the seed. Update to reflect the real state (MovementPhase + ChargePhase + FightPhase all plumb it; remaining gaps are TransportManager + MissionManager + Mathhammer; closed in this task). Mention the new determinism smoke test as the regression gate.

- [ ] **Validator + commit**

  - `export PATH="$HOME/bin:$PATH" && bash 40k/tests/run_pretrigger_tests.sh` — must stay green (was 19 tests passing baseline)
  - New determinism smoke test must show 3-runs-identical
  - Commit message: "Close remaining #329 RNG bypasses (TransportManager + MissionManager) + determinism smoke test"
