# AI Plan Maker — progress log

One line per task: status, key result, timestamp (UTC). Companion to
`.llm/plan-maker-todo.md`.

| Task | Status | Key result | When |
|---|---|---|---|
| content-1 | DONE | **First plan-content pass, done the way a player would.** Three-lens design panel (primary-max / tempo / counterpunch) adversarially judged; the synthesis corrected twice by measurement BEFORE placement (the judge's 17" Gretchin picket breaks the 9" envelope — the PM-F4 shape; "deep strikers arrive AT an earmarked objective" is not an engine behaviour — `REINFORCE_OBJ_NEAR_BASE` scores ANY objective). Authored in the LIVE Plan Editor over the bridge: real FORMATIONS declarations, every placement through validate-and-nudge on the live phase, Wartrikes placed by P1-66 itself, intents painted through the INTENTS panel handlers, saved with the banner button. Terrain rewrote the flanks (walls block MOUNTED on both zone edges; bike columns ride the clear board-edge corridors instead, Stormboyz jump the ruins). Measured in the shipped simulator, both seat orders, seeds 4001-4006: **11-1 vs the shipped hammer plan, seat-cancelled margin ≈ +21 VP/game, zero stalls**. Shipped as `orks_free_grab_grip_hammer_anvil.json` (layout-bound — coexists with the generic plan, no regression surface off its layout). Suite 2790/0 across 120; windowed 550/15 — after the pm10 shipped-list pin was updated for the third plan, and after the suite caught user://saves carrying PRE-REBUILD fixture boards: the runners' `cp -n` never refreshes a stale copy, so both runners and the rot-guard now checksum-compare and overwrite (the rot-guard catching its own copy pipeline in the wild). Report `bench_baselines/2026-08-25_free_grab_grip.md`. Version 1.38.0 | 2026-08-26 |
| PM-F7 | DONE | **The filed diagnosis (re-tune anchors) was too shallow** — the repair had a structural cause: the spike packed the two ATTACHED Wartrikes as free units, but play auto-deploys an attached character ADJACENT to its bodyguard (P1-66) at a spot authoring never validated against, so the authored and played boards disagreed by two Wartrike footprints and whichever planned unit stood on one got repaired (`U_WARBIKERS_B`: 7.17" shipped, 17.43" naive 11e re-run, twice each). Fix: **authoring is now a rehearsal** — attachments declared in state, packing in PLAY order, the phase's own P1-66 placing the Wartrikes, every later placement validated against their true footprint; Wartrike placements dropped from the emitted file. Measured vs the 10-of-11 bar: **11 of 11 exact, empty repair list, twice, 28/28** — scenario floor raised to 11. Both plans re-authored at 11e on the rebuilt boards (crucible anchors moved to the true triangle/objective). Suite 2790/0 across 120; windowed batch 550 assertions / 15 scenarios. Version 1.37.2 | 2026-08-21 |
| PM-F3 | DONE | All six 2000pt fixtures rebuilt; the rot source was the BUILDER copying the shell save's stale board verbatim. It now refreshes zones from `DeploymentZoneData` + objectives via `MissionManager` (layout-sourced objectives keep their say), packs wholly inside the zone POLYGON, and falls back to wider/narrower block shapes when the near-square fits nowhere (the true P2 triangle narrows to a point; asym's Ork side failed at 45/77 models without it). Rot-guard `test_fixture_boards_current.gd`: **12/24 FAIL on the old boards (8/36/29 deployed models outside the true zone), 24/24 on the rebuilt** — plus a measured trap: a save-restored objective's Vector2 round-trips to a different TYPE, so stringify comparison fails with every value equal. `fixture_check.py` PASS ×6; old→new sha256s + baseline supersession note in `bench_baselines/2026-08-21_fixture_regeneration.md`; the stale ork board preserved byte-for-byte as `staleboard_orks_2000_predeploy` (the only PM-F6 repro) | 2026-08-21 |
| PM-F6+F1 | DONE | **Closed by a 2×2.** The 08-20 note said the stall did not reproduce on a menu crucible game; the missing precondition was the STALE fixture board — its stepped zone is itself non-rectangular. The exact two games the PM-10 A/B lost (stale ork predeploy, arm M1, seeds 9001/9005, Normal) re-run on HEAD and on a control worktree with only the polygon-guard commit reverted: **control stalls both with the PM-10 signature to the action count (`no progress for 90s at 1|1|41/42`, plans-OFF seat); HEAD completes both as full games.** One commit of difference flips both seeds. The retry bound PM-F6 asked for already existed — the stall lived below it, in the formula proposing unfillable rectangle-derived positions. Regression nets: polygon-guard unit test 31/31 (with leak control) + `sp/pm_f1_crucible_deploys_without_retries` 13/13; repro preserved as the staleboard fixture. A/B re-run for the 6-usable-pairs half of the gate: launched, read at the end | 2026-08-21 |
| PM-F2 | DONE | Validator flags a leader attachment the game cannot make, mirroring FormationsPhase's own checks (CHARACTER, non-empty can_lead + enhancement extras, non-CHARACTER bodyguard, keyword match) as WARNINGS through the browser badge. Headless 72/72 with a true-negative control; windowed `sp/pm_f2_attachment_note_in_browser` 11/11 (badge `OK, 1 note(s)` in warning colour beside a clean control row; detail names the character and rule). Trap found: **a failed `load()` inside a static function aborts it silently** — loading CharacterAttachmentManager (uncompilable under `godot -s`) emptied every note while the identical code passed in a deferred debug harness; the enhancement extras are inlined instead. Version 1.37.1 | 2026-08-21 |
| PM-F4 | DONE | **The filed diagnosis was wrong and is corrected in place.** `_plan_positions_legal` was never edition-blind: it has called `AttackSequence.check_unit_coherency` since PM-2a, and `DeploymentPhase._check_deployment_coherency` calls the same function — its docstring calls it "the single source of truth". The real defect was an edition split between AUTHORING and PLAY: `SettingsService` pins any `godot -s` run to the 10e baseline (no 9" envelope), so the PM-10 spike certified a 13.60" Gretchin line that every consumer — player launch and `AIBenchmarkRunner` alike, both at 11 — then refused. Measured: that line is coherent at 10e and has **6 offenders** at 11e (the report's "6 of 11" was the offender count, not placements). Fix: `PlanValidator` checks coherency **pinned to edition 11** whatever the ambient setting, via the same helper, restoring the global after. Warning not error, matching the zone-polygon precedent — an over-spread unit is repaired or falls back, it is not an illegal state; it reaches players through the browser badge. Gate `tests/unit/test_plan_coherency_editions.gd` **17/17**, with a control proving the shape actually discriminates. Windowed `sp/pm_f4_coherency_note_in_browser` **14/14** — and its first run caught the fix being INVISIBLE to players: `PlanManager.list_plans()` validated with no army, so with no base sizes the coherency check declined and the badge read a clean 'OK'; `list_plans` now resolves the army from the plan's own `keys.army_file`. The headless test passed throughout because it supplies an army. Two of my own assumptions were wrong too: the status column is index 4 (5 is 'Where'), and a WINDOWED run boots at edition 10 as well — `--scenario-file=` matches `_is_automated_harness()` just like `-s`. True positive (`fixture_minimal_valid`, 8 of 11 models) and true negative (both shipped plans, 0 warnings). The spike now authors at 11 — which **rewrites hammer_anvil deterministically and makes it WORSE in play** (10→9 exact, `U_WARBIKERS_B` 7.17"→17.43", twice each), so the shipped plans were reverted and kept; filed as PM-F7 with the spike header warning against committing a re-run. Version 1.36.3 | 2026-08-20 |
| PM-F5 | DONE | The formula's embarkation pass ran unconditionally under a plan — `_evaluate_transport_embarkation` was never handed the plan at all, while the reserves pass two lines below it already checked `_plan_owns_reserves`. It now skips any unit the plan PLACES or (post-trim) RESERVES, logging each suppression. FORMATIONS is the only route: the AI never emits `COMPOSITE_DEPLOY` (`DeploymentPhase.gd:704`). Windowed `sp/pm10_shipped_plan_from_menu` **28/28**, the deliberate wrong-behaviour pin flipped to `plan=0 ai=`; eligible 9 → **11**, exact 8/9 → **10/11**, identical across two runs. Headless suite **2711 passed, 0 failed across 117 tests**. The new test needed a negative control to mean anything, and building it exposed two traps: `clear_all_plans()` does NOT mean "no plan" (auto-match re-resolves; only `PLANS_ENABLED=0` gates it), and the test snapshot had no `transport_data` (ArmyListManager derives it at load), which would have made every embarkation assertion in that file vacuous. Also corrected the filed report: only Gretchin **Alpha** is on `obj_home_1` (2.11" from the marker edge), Beta screens 6.68" away — and the capture's "Uncontrolled" label is pre-round-1 state, not a surviving bug. Version 1.36.2 | 2026-08-12 |
| PM-11 | DONE | Docs + coherence pass. New player-facing `AI_PLANS_GUIDE.md`; PLAN_FORMAT gains a 10-step walkthrough over 14 live captures. **Batch run 498 assertions across 11 scenarios, all green** — but only after it caught `pm7b_plan_browser` failing 4: it assumed the browser held exactly one plan, which PM-10's shipped content broke. The browser was right, the test was wrong; it now finds rows by name and counts only `yours`. `PLANS_ENABLED:0` proven byte-identical to no plan (6/6, with a negative control). Re-entry 36/36 — and the `plan_editor` flag DOES outlive the editor, which is harmless because Start replaces `game_config`, now proven rather than assumed. Shipping plans changed the default (auto-match): documented in 1.36.1. Full headless suite **2703 passed, 0 failed across 117 tests** — but only after it caught a REAL bug shipping content created: `find_plan_match_for` broke ties alphabetically, so a player's own plan lost to a shipped one ('Orks…' < 'PM1…'). Tie-break is now rank → source (yours wins) → name → path | 2026-08-12 |
| PM-10 | DONE | Two shipped plans (`orks_recon_stomps_crucible`, `..._hammer_anvil`), 0 errors/0 warnings, 13 placements + 4 reserves + 2 attachments = all 17 units each. Adherence **100% on both seats** (13/13 + 4/4) in every completed A/B game; windowed `sp/pm10_shipped_plan_from_menu` 27/27; export verified by parsing a real `.pck`. **E = +3.00 VP/game, se 6.83, MDE ±13.7 at n=4 — a null, reported not claimed** (at n=3 it read +9.67 with an interval excluding zero; the 4th pair was −17.0). **Stall gate FAILS**: 2 of 6 seeds hang in deployment, always on the plans-OFF seat. Four defects filed — PM-F3 stale fixture zone, PM-F4 edition-blind 9" envelope (cost 6 of 11 placements), PM-F5 the AI embarks plan-placed units so the plan's objective ends Uncontrolled, PM-F6 the stall. Version 1.36.0 | 2026-08-11 |
| PM-9 | DONE | `PlanSimulatorUI` autoload CanvasLayer (games play underneath it): configure, Run/Cancel, live progress, **measured** ETA, results table. Windowed `sp/pm9_simulator_run` 46/46 — cancel path returns to the menu, and every row + the summary is asserted against the backend's JSON. Added `wait_for_script` to ScenarioRunner after `await` in `execute_script` silently no-opped 11 assertions. Version 1.35.0 | 2026-08-11 |
| PM-8b | DONE | `PlanSimulator` autoload runs N seeded AI-vs-AI games in-process with PM-8a's full reset list; `GameWatcher` extracted (completed/timeout/stalled). Headless gate 39/39: same `seed_base` twice → identical winners, margins and VP; a different seed → a different game; seat-2 adherence 3/3 both games; 0 stalls, 0 timeouts, **0 SCRIPT ERRORs** after fixing a pre-existing `_assess_engage_on_all_fronts` Array-indexed-as-Dictionary bug | 2026-08-11 |
| PM-7b | DONE | `PlanBrowserDialog` from the menu: six columns + PlanValidator badge, rename (file moves with the name), delete behind a confirm, and shipped `res://` plans read-only (enforced in `PlanManager`, not just the UI). Windowed `sp/pm7b_plan_browser` 42/42 including "the file still exists until you confirm". Version 1.34.0 | 2026-08-11 |
| PM-7a | DONE | Per-AI-seat "AI Plan" picker in the menu (filtered by army, zone mismatch and invalid flagged), plumbed through `meta.game_config` and installed after `AIPlayer.configure()`. New `suppress_player_plan()` — "None" was silently auto-matching, caught by the scenario's control seat. Windowed `sp/pm7a_assign_plan_from_menu` 25/25 with 12 plan-sourced deployment records on the assigned seat vs 0 on the None seat. Version 1.33.0 | 2026-08-11 |
| PM-6 | DONE | INTENTS panel + non-fading board badges: five verbs, HOLD bound by clicking the objective marker (the painter hit-tests it — ObjectiveVisual has no input handling), Clear, and `RESERVE_UNTIL` reconciled into `deployment.reserves` by the recorder. Windowed `sp/pm6_paint_intents` 64/64 first run, ending with the AI's own `_plan_earmark_for` returning all five verbs off the saved file. Version 1.32.0 | 2026-08-11 |
| PM-5 | DONE | `PlanRecorder.gd` serialises a live deployment into a valid plan (order from `phase_log`, px→inches, reserves/embark/attach lifted out, anchors derived) + a `Save as Plan` dialog that refuses invalid plans in-place. Headless 47/47 with an exact record→consume round trip for both seats; windowed `sp/pm5_record_and_save_plan` 49/49 recording all 17 units at 0.0056" fidelity and round-tripping the saved FILE at 5.4e-06". Version 1.31.0 | 2026-08-11 |
| PM-4 | DONE | Main-menu "Plan Editor" opens a solo sandbox: own zone picker, `plan_editor` in `meta.game_config`, P2 units deleted, real FORMATIONS for the target army with the absent seat auto-confirmed, roll-off auto-resolved with P1 as Defender, hotseat handoff suppressed, and one guard in `PhaseManager._on_phase_completed` holds DEPLOYMENT open. Windowed `sp/pm4_plan_editor_session` 55/55 from the main menu with a negative control proving the hold comes from the flag; 0 log errors. Version 1.30.0 | 2026-08-11 |
| PM-0 | DONE | `wh40k_ai_plan` v1 schema + `PlanValidator.gd` + 2 fixtures + 65-assertion headless test (65 passed, 0 failed) | 2026-08-11 |
| PM-3 | DONE | Five verbs bias real machinery (HOLD/PUSH via `_t_add`, SCREEN withholds into the screening pass, HUNT adds to all three target scorers), release-on-damage decay, fragment layering with explicit-profile precedence. Headless 35/35 incl. a real assignment differential; windowed `sp/pm3_earmarks_bias_assignment` 72/72 with 0 earmark records in the plans-off control. Version 1.29.2 | 2026-08-11 |
| PM-2b | DONE | Plan drives FORMATIONS: reserves (plan owns them, over-cap trimmed in plan order), embarkations, attachments; PLACE_IN_RESERVES retrofit for post-formations games. Headless 27/27, windowed `sp/pm2b_formations_from_plan` 36/36 with both seats exact. Version 1.29.1 | 2026-08-11 |
| PM-2a | DONE | AI follows a plan's deployment order + per-model placement, mirroring `[44-x, 60-y]` at seat 2, degrading per unit to the formula. Headless 34/34; windowed `sp/pm2a_ai_deploys_from_plan` 41/41 with 22/22 units at 0.000" and 11 plan records per seat; `BENCH_P1_PLAN`/`BENCH_P2_PLAN` plumbing. Version 1.29.0 | 2026-08-11 |
| PM-8a | DONE | Spike: in-session multi-game reset VIABLE. 3 from-menu AI-vs-AI games in one process, ~10-14s each; two same-seed games byte-identical — but only after extending the reset list (AI quiesce first, FactionAbilityManager, global RNG as a 4th seed channel, log accumulators). Report: `tests/bench_baselines/2026-08-11_pm8a_inline_reset_spike.md` | 2026-08-11 |
| PM-1 | DONE | `PlanManager.gd` storage/listing/matching; game_config + faction-fallback identity; `_P<player>` mirror re-key resolution (55 passed, 0 failed) | 2026-08-11 |
